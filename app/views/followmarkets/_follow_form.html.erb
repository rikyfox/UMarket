<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.followingmarket?(@market) %>
    <%= render 'followmarkets/unfollow' %>
  <% else %>
    <%= render 'followmarkets/follow' %>
  <% end %>
  </div>
<% end %>
